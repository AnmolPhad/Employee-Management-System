# Employee Management Client

Separate React + Vite frontend for the Employee Management System.

## Setup

```bash
npm install
npm run dev
```

Copy `.env.example` to `.env` and update `VITE_API_BASE_URL` if the backend port changes.

The current backend launch profile uses `https://localhost:7224` and `http://localhost:5180`.
